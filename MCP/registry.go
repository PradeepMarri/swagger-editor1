package main

import (
	"github.com/swagger-editor-api/mcp-server/config"
	"github.com/swagger-editor-api/mcp-server/models"
)

func GetAll(cfg *config.APIConfig) []models.Tool {
	return []models.Tool{
	}
}
